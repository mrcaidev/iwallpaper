export type User = {
  id: string;
  email: string;
  nickName: string;
  avatarUrl: string;
};

export type Wallpaper = {
  id: string;
  thumbnailUrl: string;
  rawUrl: string;
  tags: string[];
};
